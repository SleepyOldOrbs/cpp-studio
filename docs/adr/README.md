# Architecture Decision Records

Architecture decisions affecting `cpp-studio` live in this directory.

Create ADRs only when a significant decision is resolved. Use filenames such as `0001-short-decision-title.md` and include:

- Status
- Context
- Decision
- Consequences

Do not create placeholder decisions. Supersede an accepted ADR with a new ADR instead of silently rewriting its history.
